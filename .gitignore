# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# Environments
.venv/
venv/
.env
.env.*
# ...but DO commit the template (it holds no real secrets)
!.env.example

# uv
# (uv.lock IS committed)

# Tooling caches
.pytest_cache/
.ruff_cache/
.pyright/

# Local data stores (employee-owned; never commit)
*.db
*.sqlite
*.sqlite3
data/

# Claude Code project-local settings/state
.claude/

# OS
.DS_Store

# k8s real secret (use secret.example.yaml as template)
deploy/k8s/secret.yaml
